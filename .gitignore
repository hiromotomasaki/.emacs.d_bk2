# ****** example ****** #
# # 特定の拡張子を無視する場合はどこにも/を付けない
# *.o
# # 特定のファイルを無視する場合は先頭に/を付ける
# /npm-debug.log
# # 特定のディレクトリを無視する場合は先頭と末尾に/を付ける
# /bin/
# ********************* #
/auto-save-list/
/v24.5.3/

*~
*.elc

# 反映
# 参考 : http://nekova.hatenablog.com/entry/2013/02/20/013909
# 一旦全てを対象から外す
# git rm --cached -r .
# その後全てを対象に含める
# (.gitignoreに記述されたものは除外される)
# git add -A .
# コミットする
# git commit -m ".gitignore 除外指定を変更"
